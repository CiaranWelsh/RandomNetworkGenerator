######################################################################
#   googletest
#

if (${BUILD_TESTS})
    add_subdirectory(googletest)
endif ()


if (${SIM_FEATURES})
    # then we need to locate and use roadrunner.
endif()















